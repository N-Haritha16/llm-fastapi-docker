version: "3.9"

services:
  llm_api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - MODEL_NAME=${MODEL_NAME}
      - API_KEY=${API_KEY}
      - MAX_NEW_TOKENS=${MAX_NEW_TOKENS}
